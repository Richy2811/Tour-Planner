<UserControl x:Class="TourPlanner.Views.TourList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListBox ItemsSource="{Binding TourList}" Grid.ColumnSpan="2" Margin="4,0,1,0">
            <DockPanel VerticalAlignment="Top">
                <Label Content="Tours" />
                <Button Margin="2.5,2.5,2.5,2.5" Command="{Binding AddTour}">+</Button>
                <Button Margin="2.5,2.5,2.5,2.5" Command="{Binding DeleteTour}">-</Button>
                <Button Margin="2.5,2.5,2.5,2.5" Content="[]"/>
            </DockPanel>
            <ListBoxItem>- Tour1</ListBoxItem>
            <ListBoxItem>- Tour2</ListBoxItem>
        </ListBox>
    </Grid>
</UserControl>
